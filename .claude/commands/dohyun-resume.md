---
description: After /clear or fresh session, restore dohyun working context (active task, review-pending, working tree, recent commits, next action) in one step
allowed-tools: Bash
---

Run `dohyun resume` and present the output verbatim. Do not auto-execute the
"Next action" — leave the next command to the user.
