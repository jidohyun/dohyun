---
description: Mark the current dohyun task complete (requires all DoD checked)
allowed-tools: Bash
---

Run `dohyun task complete` to finish the current in-progress task.

This fails if any DoD item is unchecked. If it fails, run `dohyun dod` to see what remains.
