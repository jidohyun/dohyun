---
description: Show recent dohyun activity log entries
argument-hint: "[--tail N] [--filter keyword]"
allowed-tools: Bash
---

Run `dohyun log $ARGUMENTS` to show recent log entries.

Use `--tail N` to limit entries, `--filter keyword` to filter by action name (e.g., `checkpoint`, `guard`, `session-start`).
