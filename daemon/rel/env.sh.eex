#!/bin/sh
# Disable Erlang distribution. The dohyun daemon is always a single local
# process communicating over a Unix domain socket — it never needs to join
# a cluster, and leaving distribution on causes node-name clashes whenever
# two harnesses on the same host try to boot.
export RELEASE_DISTRIBUTION=none
